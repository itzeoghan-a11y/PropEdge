[build]
builder = "nixpacks"

[deploy]
startCommand = "celery -A app.workers.tasks worker --loglevel=info --concurrency=4"
restartPolicyType = "on_failure"

[environments.production]
  [environments.production.deploy]
    numReplicas = 1
